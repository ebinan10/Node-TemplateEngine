extends layouts/main-layout.pug 

block styles 
     link(rel="stylesheet", href="/forms.css")

block content 
    div.main
        form(action="/", method="post").product-form
            div.form-control
                    label(for="title") Title 
                    input( type="text", name="title")#title
            button(type="submit") Add Product
        
        